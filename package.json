{
  "name": "t20-zaperas-automations",
  "version": "0.1.0",
  "description": "T20 Zapera's Automations — módulo de automações para Tormenta20",
  "scripts": {
    "pack": "fvtt package pack -n zaperas-macros --in src/packs/zaperas-macros --out packs",
    "unpack": "fvtt package unpack -n zaperas-macros --in packs --out src/packs/zaperas-macros"
  },
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^3.0.0"
  }
}
